services:
  fhir-db:
    image: postgres:15-alpine
    container_name: fhir-db
    restart: always
    environment:
      POSTGRES_DB: hapi_fhir
      POSTGRES_USER: fhiruser
      POSTGRES_PASSWORD: fhirpassword
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fhiruser -d hapi_fhir"]
      interval: 5s
      timeout: 5s
      retries: 10

  hapi-fhir:
    image: hapiproject/hapi:latest
    container_name: hapi-fhir
    ports:
      - "8080:8080"
    environment:
      spring.datasource.url: jdbc:postgresql://fhir-db:5432/hapi_fhir
      spring.datasource.username: fhiruser
      spring.datasource.password: fhirpassword
      spring.datasource.driverClassName: org.postgresql.Driver
      spring.jpa.properties.hibernate.dialect: ca.uhn.fhir.jpa.model.dialect.HapiFhirPostgresDialect
      JAVA_OPTS: "-Xms1024m -Xmx2048m"
      hapi.fhir.fhir_version: R4
    depends_on:
      fhir-db:
        condition: service_healthy
    restart: on-failure

  backend-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: backend-api
    ports:
      - "3001:3001"
    environment:
      PORT: 3001
      FHIR_SERVER_URL: http://hapi-fhir:8080/fhir
    depends_on:
      hapi-fhir:
        condition: service_started
    restart: on-failure
    volumes:
      - ./src:/app/src
      - ./package.json:/app/package.json

volumes:
  postgres_data: